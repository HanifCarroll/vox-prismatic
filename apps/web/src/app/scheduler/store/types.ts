// Legacy types file - most types are now inline in the store
// Keep only what's still needed by existing components

export enum SchedulerModalType {
  POST_SCHEDULE = 'post_schedule',
  POST_EDIT = 'post_edit',
}