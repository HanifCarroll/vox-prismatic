/**
 * Database Schema Re-export
 * Re-exports schema and types from the shared types package
 */

// Re-export everything from the shared database schema
export * from '@content-creation/types/database';
