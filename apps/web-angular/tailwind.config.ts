// Tailwind CSS v4 uses configless mode by default. This file is intentionally
// left empty to avoid Angular's legacy Tailwind auto-integration, which would
// inject the unsupported `tailwindcss` PostCSS plugin. Theme extensions should
// be defined in `src/styles.css` using the `@theme` rule instead.
export {};